```dart
TabBar(
 labelColor: Colors.red,                    // 선택되었을때 라벨의 색상
 labelStyle: TextStyle(fontSize: 15.0),     // 선택되었을때 라벨의 글자크기
 unselectedLabelColor: Colors.black,        // 선택되지 않았을때 라밸의 색상.
 indicatorColor: Colors.red,                // 밑줄의 색상을 설정한다.
 indicatorWeight: 2.5,                      // 밑줄의 두께를 설정한다.
 indicatorSize: TabBarIndicatorSize.label,  // 밑줄의 길이를 설정한다.
 labelPadding: EdgeInsets.only(top: 10.0),  // 라벨의 위아래 크기 설정.
 isScrollable: false,                       // 좌 우 스크롤의 설정.
  )
```
